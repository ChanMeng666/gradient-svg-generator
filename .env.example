# Copy this file to .env.local and fill in values as needed.
# All variables are optional — the app runs without any environment config.

# Public base URL used by URL builders, sitemaps, and social share links.
# Defaults to the production URL if unset.
# NEXT_PUBLIC_APP_URL=https://gradient-svg-generator.vercel.app
